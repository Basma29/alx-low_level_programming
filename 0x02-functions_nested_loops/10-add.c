#include "main.h"
#include <stdio.h>

/**
* add -  function that adds two integers.
* @a :the first integer.
* @b : the second integer.
*
* Return: the result.
*/

int add(int a, int b)
{
	return (a + b);
}
